function Todo({ id, title, completed}){
    return(
        <div>
            <input type="checkbox" />
            <label>{title}</label>
            <button></button>
        </div>
    )
} export default Todo;